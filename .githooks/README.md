# Git Hooks

Add them to help you keep your development cycle clean and consistent.

## Pre-commit Hook

The pre-commit hook is executed before each commit. It ensures that your code is formatted correctly and builds successfully.

## Set up

Use `just setup-dev` to set up the git hooks.
